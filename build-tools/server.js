var gulp = require('gulp');
require('./Gulpfile.js');
gulp.start('default');

// use npm start at command line to make gulp work without it being installed

// after cloning down a repo use npm install and then npm start